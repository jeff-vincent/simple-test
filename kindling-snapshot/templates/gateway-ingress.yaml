{{- if .Values.gateway.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-gateway
  labels:
    app: gateway
    {{- include "kindling-snapshot.labels" . | nindent 4 }}
  {{- with .Values.gateway.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.gateway.ingress.ingressClassName }}
  ingressClassName: {{ .Values.gateway.ingress.ingressClassName }}
  {{- end }}
  rules:
  - host: {{ .Values.gateway.ingress.host }}
    http:
      paths:
      - path: {{ .Values.gateway.ingress.path }}
        pathType: {{ .Values.gateway.ingress.pathType }}
        backend:
          service:
            name: {{ .Release.Name }}-gateway
            port:
              number: {{ .Values.gateway.port }}
{{- end }}
