apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ui
  labels:
    app: ui
    {{- include "kindling-snapshot.labels" . | nindent 4 }}
spec:
  selector:
    app: ui
  ports:
  - port: {{ .Values.ui.port }}
    targetPort: {{ .Values.ui.port }}
    protocol: TCP
